# Туториал "Work Queues"

## Что тут?

- `Worker` - код потребителя
- `NewTask` - код продьюсера, добавляет в очередь новое сообщение при запуске.

##  Заметки

`NewTask` запускается вне контейнера. Воркеры и реббит живут в контейнере.